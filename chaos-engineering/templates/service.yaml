apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaos-engineering.fullname" . }}
  labels:
    {{- include "chaos-engineering.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaos-engineering.selectorLabels" . | nindent 4 }}
