apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chaos-engineering.fullname" . }}-test-connection"
  labels:
    {{- include "chaos-engineering.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chaos-engineering.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
